import { {{pascalCase name}}Entity } from "../../domain/entities/{{kebabCase name}}.entity";
import { {{constantCase name}}_REPOSITORY, type I{{pascalCase name}}Repository } from "../../domain/repositories/{{kebabCase name}}.repository";
import { Create{{pascalCase name}}Dto } from "../dto/create-{{kebabCase name}}.dto";

import { Inject, Injectable } from "@nestjs/common";

@Injectable()
export class Create{{pascalCase name}}Service {
  constructor(@Inject({{constantCase name}}_REPOSITORY) private readonly {{pluralCamel name}}: I{{pascalCase name}}Repository) {}

  async execute(dto: Create{{pascalCase name}}Dto): Promise<{{pascalCase name}}Entity> {
    return this.{{pluralCamel name}}.create({ name: dto.name, updatedBy: "" });
  }
}
