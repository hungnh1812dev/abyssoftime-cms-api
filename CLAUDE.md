See `/docs/rules/workflow.md` for the feature workflow (spec > build > update spec/docs > cleanup), module rules (max 1000 lines, independent modules), commit, formatting, and naming conventions.

See `/docs/rules/bun.md` for the Bun runtime/tooling conventions (use Bun instead of Node.js, testing, frontend).
