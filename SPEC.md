# Spec

No active spec. The previous cycle (access token CRUD — create/list/delete/revoke — plus a standalone, unwired `ApiTokenGuard` for future `Authorization: Bearer` authentication) is implemented and documented in `/docs/documents/access-tokens.md` (and the `countReferences` fix it required in `/docs/documents/permissions.md`) — see those for module details, not here (per `/docs/rules/workflow.md`'s "Root docs" rule).
