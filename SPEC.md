# Spec

No active spec. The previous cycle (media module + storage module — `StorageAdapter` port with
`S3StorageAdapter`/`CloudinaryStorageAdapter` behind `LazyStorageAdapter`, and `media` upload/list/
delete with size/dimension checks and SHA-256 hashing) is implemented and documented in
`/docs/documents/media.md` and `/docs/documents/storage.md` — see those for module details, not
here (per `/docs/rules/workflow.md`'s "Root docs" rule).

Start the next cycle with `/spec`.
