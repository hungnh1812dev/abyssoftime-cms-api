# Spec

No active spec. The previous cycle (real email sender for OTP + password-reset — `SmtpEmailSender` via `nodemailer`, selected env-driven over `ConsoleEmailSender` when `SMTP_HOST` is set) is implemented and documented in `/docs/documents/auth.md` (and closes finding #10 in `/docs/documents/auth-issues-fix.md`) — see those for module details, not here (per `/docs/rules/workflow.md`'s "Root docs" rule).

Start the next cycle with `/spec`.
