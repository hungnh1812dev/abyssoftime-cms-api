# Spec

No active spec. The previous cycle (auth module hardening — fixed the 10 findings from the five-axis code review of register/verify-otp/login/refresh/forgot-password/reset-password: atomic `super_admin` promotion, username/email duplication races, password/username validation, per-route rate limiting, trust proxy, timing side-channels) is implemented and documented in `/docs/documents/auth-issues-fix.md` (and the corresponding updates in `/docs/documents/auth.md`) — see those for module details, not here (per `/docs/rules/workflow.md`'s "Root docs" rule).
