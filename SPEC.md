# Spec

No active spec. The previous cycle (Swagger/OpenAPI documentation — `@nestjs/swagger` bootstrap wiring at `GET /api-docs`, full `@ApiTags`/`@ApiOperation`/`@ApiResponse`/`@ApiCookieAuth` coverage across all 10 controllers and `@ApiProperty` coverage across all 19 DTOs, plus the cookie-auth-vs-bearer-auth reality check and the documentation-only response-shape DTOs this required) is implemented and documented in `/docs/documents/swagger.md` — see that file for module details, not here (per `/docs/rules/workflow.md`'s "Root docs" rule).
