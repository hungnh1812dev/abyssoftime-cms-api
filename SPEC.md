# Spec

No active spec. The previous cycle (document list API filter params — `filters[field][$op]=value`
bracket-notation query params on `GET /api/v1/documents/collection-type/:slug`, with per-field
operator/value-class validation and parameterized SQL, reusing the existing `orderBy`/`search`
allowlist pattern) is implemented and documented in `/docs/documents/document.md` — see that file
for module details, not here (per `/docs/rules/workflow.md`'s "Root docs" rule).
