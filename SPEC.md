# Spec

No active spec. The previous cycle (default permission/role seeding, the register/verify-otp/login/refresh/forgot-password auth lifecycle, and permission-slug authorization replacing the old role-level checks) is implemented and documented in `/docs/documents/{auth,roles,permissions,users}.md` — see those for module details, not here (per `/docs/rules/workflow.md`'s "Root docs" rule).

Start the next cycle with `/spec`.
