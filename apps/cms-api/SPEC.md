# Spec

No active spec. See `docs/documents/graphql.md` (`GraphQL Contract Parity Pass` section) for the completed contract-parity cycle — pluralized list query names, `documentId` args, the `<Type>List` envelope + `PaginationInput`, `in`/`notIn`/`IDFilter`/`TimeFilter` system-field filters, `and`/`or`/`not` combinators, and the new `DateTime` scalar — plus `docs/documents/document.md`'s "Filter combinators (additive, GraphQL-only)" section for the shared SQL-builder side.
