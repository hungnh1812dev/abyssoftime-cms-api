import { {{pascalCase name}}Entity } from "../../domain/entities/{{kebabCase name}}.entity";
import { {{constantCase name}}_REPOSITORY, type I{{pascalCase name}}Repository } from "../../domain/repositories/{{kebabCase name}}.repository";
import { Update{{pascalCase name}}Dto } from "../dto/update-{{kebabCase name}}.dto";

import { Inject, Injectable, NotFoundException } from "@nestjs/common";

@Injectable()
export class Update{{pascalCase name}}Service {
  constructor(@Inject({{constantCase name}}_REPOSITORY) private readonly {{pluralCamel name}}: I{{pascalCase name}}Repository) {}

  async execute(documentId: string, dto: Update{{pascalCase name}}Dto): Promise<{{pascalCase name}}Entity> {
    const existing = await this.{{pluralCamel name}}.findById(documentId);
    if (!existing) {
      throw new NotFoundException(`{{pascalCase name}} "${documentId}" not found`);
    }

    return this.{{pluralCamel name}}.update(documentId, { name: dto.name, updatedBy: "" });
  }
}
